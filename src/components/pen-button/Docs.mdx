import { Canvas, Meta, Controls, Title, Primary } from '@storybook/blocks';
import * as PenButtonStories from './pen-button.stories';

<Meta of={PenButtonStories} />
<Title>Button</Title>

A checkbox is a square box that can be activated or deactivated when ticked.

<Primary />
<Controls />
